import React from 'react';
import BorderColorIcon from '@mui/icons-material/BorderColor';

export default function Header() {
    return (
        <header>
            <h1>Notation {<BorderColorIcon />}</h1>
        </header>

    )
}
